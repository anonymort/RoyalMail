# Copy this file to .env.local and adjust values as needed

# --- Database configuration ---
# Use Postgres in production (Railway managed instance).
# Example for connecting to the Railway internal hostname:
# DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
# To keep local dev on SQLite, leave DATABASE_URL unset.

# DATABASE_URL=

# --- Analytics ---
# Optional override if you want to use a different GA property than the default fallback.
# NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Optional: point SQLite to a custom path when testing locally.
# SQLITE_PATH=data/delivery.sqlite
